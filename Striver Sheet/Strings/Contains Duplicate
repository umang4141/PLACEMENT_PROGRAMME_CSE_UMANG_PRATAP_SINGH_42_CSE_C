class Solution {
    public boolean containsDuplicate(int[] nums) {
            HashSet<Integer>h1 =  new HashSet<Integer>();
        for(int i=0;i<nums.length;i++)
        {
            h1.add(nums[i]);
        }
        if(nums.length==h1.size())
            return false;
        else
            return true;
    }
}
